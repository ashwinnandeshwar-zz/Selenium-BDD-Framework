package pageObjects;import dataProviders.ConfigFileReader;import org.apache.logging.log4j.LogManager;import org.apache.logging.log4j.Logger;import org.openqa.selenium.JavascriptExecutor;import org.openqa.selenium.Keys;import org.openqa.selenium.WebDriver;import org.openqa.selenium.WebElement;import org.openqa.selenium.support.FindBy;import org.openqa.selenium.support.PageFactory;import org.openqa.selenium.support.ui.WebDriverWait;import stepDefinations.CurrencyConversionStepDef;import utilities.WaitHandlers;import java.util.concurrent.TimeUnit;public class CurrencyConversionPage {    WebDriver driver;    ConfigFileReader configFile;    WebDriverWait check;    public CurrencyConversionPage(WebDriver webDriver) {        this.driver=webDriver;        this.configFile= new ConfigFileReader();        PageFactory.initElements(webDriver, this);       // PageFactory.initElements(new AjaxElementLocatorFactory(webDriver, 15), this);        webDriver.manage().timeouts().pageLoadTimeout(10, TimeUnit.SECONDS);    }    @FindBy(id = "amount")    private WebElement amountToConvert;    @FindBy(id = "midmarketFromCurrency")    private WebElement fromCurrency;    @FindBy(id = "midmarketToCurrency")    private WebElement toCurrency;    @FindBy(xpath = "//button[contains(text(),'Convert') and @type='submit']")    private WebElement convertBtn;    @FindBy(xpath = "//button[contains(text(),'Accept')]")    private WebElement cookiePopup;    @FindBy(xpath = "//button[@role='button' and @aria-label='close']")    private WebElement secondPopup;    public void setAmountToConvert(String amntToConvert){        WaitHandlers.waitMilliseconds(3000);        //code to handle cookie based pop up        if(cookiePopup.isDisplayed()){            cookiePopup.click();            WaitHandlers.waitMilliseconds(1000);            ((JavascriptExecutor) driver).executeScript("arguments[0].click();", amountToConvert);        }        //handlePopups();        this.amountToConvert.sendKeys(amntToConvert);        //code to handle second pop        WaitHandlers.waitMilliseconds(10000);        if(secondPopup.isDisplayed()){            secondPopup.click();            WaitHandlers.waitMilliseconds(2000);        }    }    public void setFromCurrency(String frmCrny){        fromCurrency.click();        WaitHandlers.waitMilliseconds(1000);        fromCurrency.sendKeys(frmCrny);        fromCurrency.sendKeys(Keys.ENTER);    }    public void setToCurrency(String toCrny){        toCurrency.click();        WaitHandlers.waitMilliseconds(1000);        toCurrency.sendKeys(toCrny);        toCurrency.sendKeys(Keys.ENTER);    }    public void convert(){        convertBtn.click();    }}